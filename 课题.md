### DWT 核心业务流程端到端 AI 自动化测试建设

#### 当前痛点/现状（描述耗时点 、易错点 、业务瓶颈等）
```txt
Dowalet 当前测试人力有限，每次登录、注册、忘记密码、KYC、钱包首页、消息中心、账号安全和后台审核等功能变更后，都需要研发或业务人员重复人工回归。

人工回归主要痛点：
1. 核心链路长：user 端提交后经常还需要 admin 端审核或回查状态。
2. 操作重复：登录、表单填写、验证码、上传、列表查询和弹窗确认耗时明显。
3. 易漏测：页面跳转、接口成功后的页面展示、错误提示、空态和异常态容易漏掉。
4. 定位难复现：失败时缺少统一日志、截图、trace 和报告，研发排查成本高。
5. 业务知识沉淀不足：测试步骤分散在个人经验里，缺少可复用、可持续维护的用例库。
```

#### 拟使用的AI工具 （写明具体工具+应用环节）
```txt
使用 Codex / Claude Code / Cursor + Playwright + DWT Testing 平台建设前端端到端自动化测试。

AI 应用环节：
1. 业务流程梳理：根据需求文档、前端路由、页面源码和接口文档整理核心回归路径。
2. 用例生成：辅助生成 YAML DSL 场景、页面定位建议和断言点。
3. 脚本执行：通过 DWT Testing Runner 调用 Playwright 执行 Web 流程，并支持 API 断言和只读 DB 校验。
4. 失败分析：结合失败截图、接口诊断、日志和 trace 输出复现步骤与修复建议。
5. 文档沉淀：生成测试数据准备说明、异常场景清单、执行说明和 AI 提示词模板。

当前已覆盖和规划覆盖的场景包括：user/admin 登录、注册、忘记密码、KYC 提交与后台审核、钱包首页、消息中心、后台资料修改、手机号绑定、验证方式修改、表单提交、弹窗交互和列表渲染。
```

#### 预期达成效果（可量化/可验证的数字/成果目标，请分条列述）
```txt
1. 建成一套可重复执行的 Dowalet 前端自动化回归用例库，核心用例以 YAML DSL 维护，避免测试逻辑散落在临时脚本中。
2. 覆盖 P0 核心流程：user 登录、admin 登录、KYC 提交与审核、钱包首页、消息中心。
3. 覆盖 P1 扩展流程：注册、忘记密码、手机号绑定、验证方式修改、后台个人资料修改。
4. 每条用例至少包含页面入口、关键操作、接口等待或业务码校验、页面断言和失败产物。
5. 将核心流程人工回归测试时间减少 50% 以上；登录、钱包首页、消息中心等冒烟用例可在数分钟内重复执行。
6. 失败时自动保留日志、截图、trace、视频和 HTML/JSON 报告，降低研发复现和定位成本。
7. 形成可交付文档：用例矩阵、测试数据准备说明、页面断言与异常清单、执行说明、AI 提示词模板。
```

#### 交付物
```txt
1. 一套基于 Playwright + YAML DSL 的前端端到端自动化测试平台与脚本。
2. Dowalet 核心业务流程测试用例清单：docs/dowalet-regression-case-matrix.md。
3. 前端测试数据准备说明：docs/test-data-preparation.md。
4. 页面断言和异常场景清单：docs/assertion-and-exception-checklist.md。
5. 自动化测试执行与参赛演示说明：docs/execution-and-demo-guide.md。
6. AI 辅助生成测试用例和脚本的提示词模板：docs/ai-prompt-templates.md。
7. 可执行 YAML 用例：cases/scenario/ 下的登录、注册、忘记密码、KYC、钱包首页、消息中心、账号安全和后台资料修改场景。
8. 测试运行产物：reports、screenshots、traces、videos 中生成的 HTML/JSON 报告、截图、trace 和视频；对外展示前进行脱敏。
```
